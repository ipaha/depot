<tr>
  <td><%= line_item.quantity %>&times;</td>
  <td><%= line_item.product.title %></td>
  <td class="item_price"><%= number_to_currency line_item.price %></td>
</tr>
<tr>
  <td />
  <td><%= button_to 'Delete', line_item.cart.line_items.count > 1 ? line_item : line_item.cart, method: :delete, confirm: 'Are you sure?' %></td>
  <td />
</tr>
